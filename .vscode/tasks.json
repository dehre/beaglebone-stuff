{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "cmake clean",
            "command": "rm -rf build",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
